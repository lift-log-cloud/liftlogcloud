apiVersion: apps/v1
kind: Deployment
metadata:
  name: core
  namespace: liftlog
spec:
  replicas: 1
  selector:
    matchLabels:
      app: core
  template:
    metadata:
      labels:
        app: core
    spec:
      containers:
      - name: core
        image: liftlogcloud-core:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 25590
        env:
        - name: SERVICE_NAME
          value: "core"
        - name: DATABASE_URL
          valueFrom:
            configMapKeyRef:
              name: liftlog-config
              key: DATABASE_URL
        - name: STATS_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: liftlog-config
              key: STATS_SERVICE_URL
        - name: SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: liftlog-secrets
              key: SECRET_KEY
        readinessProbe:
          httpGet:
            path: /health
            port: 25590
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 25590
          initialDelaySeconds: 15
          periodSeconds: 20
        resources:
          requests:
            cpu: "150m"
            memory: "256Mi"
          limits:
            cpu: "800m"
            memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: core
  namespace: liftlog
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/metrics"
    prometheus.io/port: "25590"

spec:
  type: LoadBalancer
  selector:
    app: core
  ports:
  - name: http
    port: 25590
    targetPort: 25590